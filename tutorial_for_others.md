# 🚀 VICUNA AI 多維度再購預測與自動化系統介紹

## 💡 這是什麼？

這是一套**基於大型語言模型 (LLM) 微調技術**的智能決策系統。我們將開源的 `Vicuna-7B` 模型加上獨家領域資料進行深度訓練（LoRA Fine-Tuning），使它能根據顧客的**高階信任度四大維度**，精準判斷該顧客未來是否會再次購買。

### 📊 系統的四大預測指標 (BICT)：
傳統分析往往只看購買次數，而我們的模型深入解析了更深層的情感與信任維度：
1. **Benevolence (善意度/B)**：品牌是否展現對顧客的關心與友善支援。
2. **Integrity (誠信度/I)**：商業行為是否誠實、透明、言出必行。
3. **Competence (能力度/C)**：服務或產品的真實效能與專業程度。
4. **Trust (信任度/Trust)**：由上述三項綜合出的核心感受。

模型會根據這四個分數進行交叉推論，給出最終結論：**會再購 (Purchase)** 或 **不會再購 (Not Purchase)**。

---

## ⚙️ 系統有什麼功能？它可以怎麼用？

為了滿足「即時自動化客服」以及「大數據報表分析」兩種截然不同的場景，這套系統提供了兩種核心運作模式：

### 模式一：即時 API 預測與 n8n 流程自動化
**適合場景**：自動寄發挽回信件、客服系統串接
*   **API Server**：系統可以在本機端掛載運行預期 `http://localhost:8000/predict` 的推論端點。
*   **互動自動化 (n8n)**：只要有新客戶資料流入，系統就能自動丟給 AI 模型算命。如果預測「不會再購」，立刻自動觸發信箱，寄出折價券信件或是客服問候信件來挽回客人！

### 模式二：離線大數據跑表與 Power BI 整合 (手動批次)
**適合場景**：行銷部門月報、Power BI 儀表板檢視、大量問卷數據計算
*   **一鍵預測腳本 (`predict_excel_offline.py`)**：如果你有一份從問卷蒐集來的 `客戶 BICT 評分表.xlsx`，你可以直接把這個 Excel 丟給腳本。
*   **Power BI 完美輸出**：腳本不僅會把幾千人的預測結果直接寫回 Excel，還會**自動生成一張「Summary Statistics (統計摘要)」工作表**！你接著只要把這份報告丟進 Power BI，不到一秒鐘就能拉出超精美的銷售預測 Dashboard。

---

## 🛠️ 教學：如何跑一次酷炫的批次 Excel 預測體驗？

如果你想親眼看看它怎麼運作的，請跟我這樣做（這是在本地端不需要網路就能完成的 AI 演算喔！）：

### 1. 準備檔案
確保檔案總管內有：
*   **測試名單**：`sample_bict_data.xlsx`（包含顧客的 B, I, C, Trust 分數）
*   **預測腳本**：`predict_excel_offline.py`

### 2. 下達指令
打開終端機 (Anaconda Prompt)，切換到專案資料夾後，複製並貼上這段咒語並按下 Enter：
```bash
conda run --no-capture-output -n vacuna_env python predict_excel_offline.py -i sample_bict_data.xlsx -o powerbi_dashboard_data.xlsx
```

### 3. 等待見證奇蹟
你會在畫面上看到黑色的進度條跑動：
```text
[LOAD] 初始化模型與 Tokenizer...
   [GPU] Loading with 4-bit quantization...
[OK] 模型載入完成！

預測進度: 100%|██████████| 10/10 [00:08<00:00, 1.25it/s]
[OK] 處理完成！🎉
```

### 4. 打開結果！
這時資料夾會蹦出一個新的檔案 **`powerbi_dashboard_data.xlsx`**。
把它打開看看——第一頁是包含了 AI 推論結果的名單，第二頁則是統計摘要！趕緊把它匯入 Power BI，展示給大家看這份基於先進自然語言模型產出的超準確預測吧！🚀
